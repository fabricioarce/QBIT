# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# Database Configuration
# For external PostgreSQL container, use this format:
# postgresql://username:password@postgres_host:5432/database_name
#
# Examples:
# - If PostgreSQL is in another Docker container on same network:
#   DATABASE_URL=postgresql://botuser:yourpassword@postgres-container-name:5432/botdb
#
# - If PostgreSQL is on same Docker host but different network:
#   DATABASE_URL=postgresql://botuser:yourpassword@172.17.0.2:5432/botdb
#
# - If PostgreSQL is on external host:
#   DATABASE_URL=postgresql://botuser:yourpassword@db.example.com:5432/botdb
DATABASE_URL=postgresql://postgres:password@postgres:5432/discord_bot

# Logging Configuration (optional)
RUST_LOG=info
RUST_BACKTRACE=0

# Coolify Notes:
# 1. Set these environment variables in Coolify's service settings
# 2. For DATABASE_URL, use the PostgreSQL container's service name or IP
# 3. Ensure both containers are on the same Docker network if using container names
